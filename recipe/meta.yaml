{% set name = "snowpark-connect-deps-2" %}
{% set version = "3.56.5" %}
{% set python_min = "3.10" %}
{% set python_max = "3.13" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/snowpark_connect_deps_2-{{ version }}.tar.gz
  sha256: 092348dbf527d4067adf1029a304a84f6e8e474feec572a3c574e1e9dd0b46eb

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - python >={{ python_min }},<{{ python_max }}

test:
  imports:
    - snowpark_connect_deps_2
  commands:
    - pip check
  requires:
    - python {{ python_min }}
    - pip

about:
  home: https://www.snowflake.com
  summary: Supporting JAR dependencies for Snowpark Connect (Part 2)
  license: Apache-2.0
  license_file:
    - LICENSE-binary
    - NOTICE-binary
    - LICENSE.txt

extra:
  recipe-maintainers:
    - sfc-gh-azhan
    - sfc-gh-ckonigsmark
    - sfc-gh-igarish
    - sfc-gh-kgadomski
    - sfc-gh-pwojcik
    - sfc-gh-saroskar
    - sfc-gh-skumbham
